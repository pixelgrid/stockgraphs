# Optional: IANA zone for Finviz CSV datetimes without a timezone (default America/New_York)
# VITE_FINVIZ_QUOTE_TZ=America/New_York
#
# Finviz export auth token is configured in the app Settings (localStorage), not here.
